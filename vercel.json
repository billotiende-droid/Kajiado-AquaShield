{
  "buildCommand": "pnpm run build",
  "devCommand": "pnpm run dev",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "regions": ["sfo1"],
  "env": {
    "NEXT_PUBLIC_API_BASE_URL": {
      "description": "Backend API base URL (e.g., https://your-railway-app.railway.app)",
      "required": false
    }
  },
  "public": true
}
